# H7MS-Core-A1归档说明

当前有效内容在项目根目录与docs/维护。归档包是日期快照，不能用来判定电路已通过评审或硬件已完成测试。

## 1. 归档索引

| 日期 | 归档文件 | 类型与版本 |
| --- | --- | --- |
| 2026-09-11 | `H7MS-Core-A1_2026-09-11_docs-v0.7.1.zip` | 整理后设计文档快照；Docs v0.7.1，Pin Plan v0.7，Stack Interface v0.7 |

ZIP同目录附带同名`.manifest.json`与`.sha256`文件。此索引采用文件名，避免解压后的文档副本把归档包自身当成必须随包重复存储的附件。

## 2. 快照范围与来源

- 包含项目README、CHANGELOG、project.json、docs/全部当前Markdown文档和本说明。
- ZIP内另附`H7MS-Core-A1/ARCHIVE-MANIFEST.json`，记录每个源文件的相对路径、字节数及SHA-256。
- 不递归包含其他归档包、外部校验文件或Git数据库；不包含兄弟项目XGO-Lite2。
- 外部manifest与包内manifest内容相同，ZIP自身哈希另存于`.sha256`，避免自引用。
- 整理前Git参照为`a313895e6f0785b6c3dcc8b0144dec3099ed7abd`；快照还包含2026-09-11本次整理的工作区变更，因此不能仅用该提交哈希重建快照。
- 这是文档基线归档，不含尚未建立的EDA工程、BOM、固件、模型与测试结果。

## 3. 校验与恢复

1. 对ZIP计算SHA-256，与同名`.sha256`第一列比较。PowerShell示例：

   ```powershell
   Get-FileHash -Algorithm SHA256 -LiteralPath './H7MS-Core-A1_2026-09-11_docs-v0.7.1.zip'
   ```

2. 解压到新的空目录，避免覆盖当前工作目录。ZIP以`H7MS-Core-A1/`为顶层目录。
3. 按包内`ARCHIVE-MANIFEST.json`的`files`列表核对源文件路径、长度和SHA-256。清单记录的是源文件，不把清单自身加入哈希列表。
4. 从解压后的README进入文档；先检查项目状态、未决问题和版本，再决定是否采纳历史内容。

哈希可用于一致性与意外损坏检查，不等同于数字签名。归档索引只描述文件位置；包内不再包含自身ZIP或其外部伴随文件。

## 4. 后续归档

使用新的日期/版本文件名保留旧快照。生成前先核对工作区变化、文档链接和版本，列出明确包含的文件；生成后逐项核验ZIP条目、文件哈希与外部清单一致。硬件或固件进入发布阶段后，应扩大清单范围并记录可复现的工具链、源工程、构建产物与验证报告。
